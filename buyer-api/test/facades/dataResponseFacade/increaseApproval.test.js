import test from 'ava';
// import { checkAllowance } from '../../../src/facades';
test.skip('sends tx if minimumAllowance is less than config.allowance', t => t.fail());
test.skip('does not send tx if minimumAllowance is greater than config.allowance', t => t.fail());
