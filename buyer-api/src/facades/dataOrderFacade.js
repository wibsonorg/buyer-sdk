const createDataOrder = async ({
  buyerAddress,
  filters,
  dataRequest,
  price,
  initialBudgetForAudits,
  termsAndConditions,
  buyerUrl,
  buyerPublicKey,
}) => ({ foo: 'bar' });

export default createDataOrder;
